package app.utils.annotations;

public interface AnnotationUtils {

    static String[] getAnnotatedFields(final Class<?> entityType) {
        // todo: collect all annotated for search fields
        return new String[0]; // {name:...,type:...}
    }
}
